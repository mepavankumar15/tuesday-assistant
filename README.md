# FRIDAY AI Assistant
A production-grade, Alexa-style conversational AI assistant built with LangGraph, XAI Grok, Streamlit, and ReAct Agents.

## Setup
1. `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and fill in keys.
3. `streamlit run app.py`
